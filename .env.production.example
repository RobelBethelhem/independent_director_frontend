# Frontend production env (set this in the Vercel dashboard, NOT committed).
# Point it at your deployed backend's API base — include the /api/v1 suffix.
VITE_API_BASE_URL=https://independent-director-backend.onrender.com/api/v1
